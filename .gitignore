# embedded vector store
memory/chroma_db/
knowledge_db/
chroma_db/

# pulled in repos
repos/

# secrets
.env
memory/.channel/

# venv
.venv
.venv-fc/

# editor backup files
*~
Autotests/venv/
Autotests/__pycache__/
__pycache__/
*.pyc
Autotests/logs/

# IDE
.idea/

# reasoning traces (runtime, Issue #7)
memory/traces/

# Benchmarks + FreeCiv harness were extracted to the OmegaClaw-Bench repo. Ignore any
# leftover local run artifacts (some are created root-owned by the docker runtime).
benchmarks/
# throwaway experiment build (in-container model prefetch)
Dockerfile.reason

# installed filesystem skills (runtime, Issue #12): the default skill root ships only its
# README; installed bundles + lock/origin metadata are runtime artifacts, never committed.
/skills/*
!/skills/README.md

# skill workshop proposal queue (runtime, Issue #14)
memory/skill-workshop/

# session store (runtime, Issue #16)
memory/sessions.db
memory/sessions.db-*

# scheduler jobs store (runtime, Issue #17)
memory/jobs.db
memory/jobs.db-*
